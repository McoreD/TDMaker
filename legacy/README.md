# Legacy Archive

This folder contains the original Windows-era implementation preserved for reference during migration.

- `TDMaker`: legacy WinForms/Mono solution and supporting libraries
- `TDMaker.Wpf`: older WPF shell
- `Update.xml`: legacy updater manifest

These files are not part of the modern build. The active solution is `TDMaker.slnx` at the repo root.
